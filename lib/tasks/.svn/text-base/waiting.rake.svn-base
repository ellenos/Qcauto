namespace :monitor do
  desc "Check that no job has been running more than 1 minute"
  task :waiting => :environment do
    w = DelayedMonitor.waiting
    #puts "waiting count is " + w.count.to_s
    #stuck.each do job
    	#puts "Running too long!!"
    	#puts "late job: " + job.id.to_s
      #puts "first_started at is " + job.run_at.to_s
    #end
  end
end